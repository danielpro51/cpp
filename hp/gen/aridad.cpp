#include <iostream>
/*
Los literales constantes son valores que están directamente insertados y no cambian
La aridad es la cantidad de operandos que tiene un operador
Aridad del Operador hay 3 tipos:
    - Unario: Operador que tiene un solo operando: +, -, !, ~, ++, --, sizeof, &, *, (type), new, delete
    - Binario: Operador que tiene dos operandos: +, -, *, /, %, <<, >>, <, >, <=, >=, ==, !=, &, |, ^, &&, ||, =, +=, -=, *=, /=, %=, <<=, >>=, &=, |=, ^=, ?:, , (coma)
    - Ternario: Operador que tiene tres operandos: x ? y : z

Ejemplo
    Unario: -3
    Binario: 3 + 4
    Ternario: 3 > 4 ? 3 : 4
*/
//Funciones
void imprimir(int x) {
    std::cout << x << "\n";
}
//El inicializador
int main() {
    return 0;
}