#ifndef ENVIADOR_H //Esto sirve para que no se dupliquen las funciones
//Si no está definido, el compilador lo procesará
#define ENVIADOR_H //Define la variable para que no se dupliquen las funciones

int suma(int a, int b);
#endif